apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: perfect-charge-pvc
  namespace: perfect-charge
spec:
  storageClassName: "nfs-client"
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi